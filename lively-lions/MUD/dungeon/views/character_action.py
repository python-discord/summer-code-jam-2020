# from django.shortcuts import render
# from django.http import HttpResponse
# from django.contrib.auth.decorators import login_required

# # Character action

# @login_required
# def move(request, direction):
#     return HttpResponse(f"Move with direction: {direction}")

# @login_required
# def move_to(request, object_id):
#     return HttpResponse(f"Move to object with id: {object_id}")

# @login_required
# def interact_with(request, object_id):
#     return HttpResponse(f"Interact with object with id: {object_id}")

# @login_required
# def look_at(self, object_id):
#     return HttpResponse(f"Look at object with id: {object_id}")
