/* Command parsing prototypes, not sure where these should go

They are supposed to help tell commands that should be sent via
websocket vs the ones that should be sent via REST.

REST seems to have authentication built in, whereas websockets
do not? I don't know much about the reasoning behind this, but
I can write the functions well enough.

*/

isAdminCommand = (s) => {

}
