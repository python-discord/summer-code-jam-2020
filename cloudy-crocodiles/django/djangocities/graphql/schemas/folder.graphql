type Folder {
    id: ID!
    site: Site!
    parent: Folder
    path: String!
}

type Item {
    id: ID!
    # folder: Folder!
    filename: String!
    collection: String!
}

input FolderInput {
    site: ID!
    parent: ID
    path: String!
}
