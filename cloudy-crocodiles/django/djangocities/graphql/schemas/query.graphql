type Query {
    allUsers: [User!]!

    allCities: [City!]!
    city(slug: String!): City!
    citySites(id: ID!): [Site!]!
    citySite(slug: String!, address: Int!): Site!

    allSites: [Site!]!
    site(id: ID!): Site!
    siteFolder(id: ID!, path: String!): Folder!

    allFolders: [Folder!]!
    folder(id: ID!): Folder!
    folderData(id: ID!): String!

    allPages: [Page!]!
    page(id: ID!): Page!

}
